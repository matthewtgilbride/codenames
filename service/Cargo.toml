[workspace]
resolver = "2"
default-members = [
    "domain",
    "common",
    "lambda",
]
members = [
    "domain",
    "common",
    "lambda",
]

[profile.release]
# Optimize for small code size
opt-level = "s"
lto = true

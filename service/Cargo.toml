[workspace]
resolver = "2"
default-members = [
    "domain",
    "common",
    "actix",
    "lambda",
]
members = [
    "domain",
    "common",
    "actix",
    "lambda",
]

[profile.release]
# Optimize for small code size
opt-level = "s"
lto = true
